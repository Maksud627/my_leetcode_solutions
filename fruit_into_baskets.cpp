
// 904. Fruit Into Baskets: https://leetcode.com/problems/fruit-into-baskets/description/
// medium, two pointer


/*
start both pointer from the start of the array
when there are 3 types of fruit in the map, increase the left pointer as it may remove one type
other case increase the right pointer
store max length until reach the end
 */

class Solution {
public:
    int totalFruit(vector<int>& fruits) {
        // [1,2,3,2,2]
        // start, end, max_len
        
        int left = 0;
        int right = 0;
        int maxLength = 0;
       
        while(right < fruits.size()){

        }

        return maxLength;
    }
};




